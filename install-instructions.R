# Run the code below on your own computer to 
# install the packages that we used in class today.

install.packages(c("babynames", "formatR", "gapminder", 
                   "hexbin", "mgcv", "maps", "mapproj", 
                   "nycflights13", "rmarkdown", "tidyverse", 
                   "viridis"))